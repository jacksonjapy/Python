"""
Write By Jackson
"""


if __name__ == '__main__':
    # a = [1, 2, 3, 4, 5]
    # b = list(map(lambda x: x * x, a))
    # print(b)
    name = ["Littlefive", "amanda", "hellokitty"]
    # 将字符串转换为小写
    last_name = list(map(str.lower, name))
    print(last_name)
